# Architech Environment Variables
# SECURITY CRITICAL: Copy this file to .env and update ALL values
# NEVER use these default values in production!

# Database Configuration - CHANGE THESE VALUES!
POSTGRES_DB=architech
POSTGRES_USER=architech
POSTGRES_PASSWORD=CHANGE_ME_$(date +%s)

# JWT Configuration - GENERATE A SECURE SECRET!
JWT_SECRET=CHANGE_ME_GENERATE_SECURE_JWT_SECRET

# Service Configuration
API_GATEWAY_PORT=8000
USER_SERVICE_PORT=8001
PROJECT_SERVICE_PORT=8002
DESIGN_SERVICE_PORT=8003
SIMULATION_SERVICE_PORT=8004
SIMULATION_ENGINE_PORT=8080
FRONTEND_PORT=3000

# Redis Configuration
REDIS_URL=redis://redis:6379

# Kafka Configuration
KAFKA_BROKERS=kafka:9092
ZOOKEEPER_CLIENT_PORT=2181

# Development/Production Mode
ENVIRONMENT=development

# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Generate secure values:
#    - POSTGRES_PASSWORD: Use openssl rand -base64 32
#    - JWT_SECRET: Use openssl rand -base64 64
# 3. Or run: ./setup-env.sh (Linux/Mac) or setup-env.bat (Windows)